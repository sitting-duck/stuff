import QtQuick 2.0

//A style for UI Element titles.  An example use would be if there are several
//columns in a dialog with different titles designating their purpose.

Text {

    font.bold: true
    font.pointSize: 12
    color: "#FFFFFF"

    anchors.bottomMargin: 10
    anchors.topMargin: 10
    anchors.leftMargin: 10
    anchors.rightMargin: 10

}
