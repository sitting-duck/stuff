```bash
sudo apt-get install jenkins

# Edit the path for Jenkins
sudo vim /etc/init.d/jenkins

Fixes ERROR: No Java executable found in current PATH: /bin:/usr/bin:/sbin:/usr/sbin
Failed to start LSB: Start Jenkins at boot time.

```

